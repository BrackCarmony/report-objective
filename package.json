{
  "name": "devmtn-report-objective",
  "version": "0.3.0",
  "description": "This is a node module to facilitate talking with the DevMountain objective tracker.  This should probably only used for DevMountain projects.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d es5"
  },
  "author": "Brack Carmony",
  "license": "MIT",
  "dependencies": {
    "jsonwebtoken": "^7.1.6"
  },
  "devDependencies": {
    "babel-polyfill": "^6.9.1"
  }
}
